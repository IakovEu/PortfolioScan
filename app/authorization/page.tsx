import { BlockMain } from '@/components/authorizationPageComponents/BlockMain';
import { Layout } from '@/components/Layout';

export default function Authorization() {
	return (
		<Layout>
			<BlockMain />
		</Layout>
	);
}
